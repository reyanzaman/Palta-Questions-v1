const version = "1.86"
const baseURL = 'http://localhost:8081/';
const alternateURL = 'https://iub-qbl.onrender.com';

const global = {
    version,
    baseURL,
    alternateURL
}

export default global;